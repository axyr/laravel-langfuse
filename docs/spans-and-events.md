[Back to documentation](README.md)

# Spans and Events

## Spans

Spans track non-LLM operations within a trace - database queries, API calls, processing steps:

```php
use Axyr\Langfuse\Dto\SpanBody;

$span = $trace->span(new SpanBody(
    name: 'retrieve-context',
    environment: 'production',
));

// ... do work ...

$span->end(output: 'Retrieved 5 documents');
```

**`end()` is what sends the span.** Creating one records the start time and nothing else; the finished observation is handed to the batcher when it ends, and is exported exactly once. A span that never ends does not appear in Langfuse - unless the application shutdown hook ends it, which marks it `WARNING` with the status message `ended by shutdown`. Ending twice is ignored and logs a warning.

`end()` accepts `output`, `statusMessage`, `level` (an `ObservationLevel` enum) and `endTime` (auto-generated by default).

Spans nest. Call `$span->span()`, `$span->generation()`, or `$span->event()` to create child observations. Use `$span->getId()` and `$span->getTraceId()` to get the IDs.

## Observation types

Langfuse v4 recognises more than span, generation and event. Pass a `type` to mark what a span actually did, so it is filterable in the UI and in the observations API:

```php
use Axyr\Langfuse\Enums\ObservationType;

$span = $trace->span(new SpanBody(
    name: 'web-search',
    type: ObservationType::Tool,
));
```

Available: `Span` (the default), `Generation`, `Event`, `Agent`, `Tool`, `Chain`, `Retriever`, `Evaluator`, `Embedding`, `Guardrail`.

## Events

Lightweight observations for discrete moments. No start/end lifecycle, so they are exported immediately with equal start and end times:

```php
use Axyr\Langfuse\Dto\EventBody;

$trace->event(new EventBody(
    name: 'cache-hit',
    input: ['key' => 'user-profile-42'],
    output: ['cached' => true],
    environment: 'production',
));
```

Events can also be created on spans via `$span->event()`.

---

Previous: [Generations](generations.md) | Next: [Scores](scores.md)
